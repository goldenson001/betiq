{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/select-schema.mjs && prisma generate && prisma db push --accept-data-loss && next build",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/trigger?phase=all",
      "schedule": "0 22 * * *"
    }
  ]
}
